# 后端地址（服务端 rewrites 使用，容器内填 http://backend:8000）
BACKEND_URL=http://127.0.0.1:8000

# 浏览器直接访问后端时使用；留空表示走同源（由 Next.js rewrites / Nginx 代理）
NEXT_PUBLIC_API_BASE=

# WebSocket 网关地址前缀，留空则默认推导为 ws://<当前域名>:8000
NEXT_PUBLIC_WS_BASE=
